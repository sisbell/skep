// MOVED to `src/canon.rs` (2026-09-05, PUB round 1 delta 2): the transcode is
// now compiled whatever the `dump` feature says, because the exception set's
// seed reads M3's publication map through it. No `mod` declaration names this
// file, so it is not compiled; it is an orphan left only because this change
// could not remove files, and should be deleted.
